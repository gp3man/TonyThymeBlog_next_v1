import { atom } from "recoil";
export const byMealState = atom({
  key: "mealState",
  default: [],
});
export const byDietaryState = atom({
  key: "dietaryState",
  default: [],
});
export const byMethodState = atom({
  key: "methodState",
  default: [],
});
export const byProteinState = atom({
  key: "proteinState",
  default: [],
});

