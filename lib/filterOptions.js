import { client } from "@/lib/contentful";

export default async function filterData(recipes) {
  // const { items  } = await client.getEntries({ content_type: "byMeal" });
  // console.log(items[0].fields);
  // const {meal, dietary, method, protein} = filter

  // const mealData = recipes.map((x)=>{
  //   x.fields.filterMeal
  // });
  // console.log(mealData)
  // // console.log(recipes)
  return null;
}
